# Copy to sifi_2026/.env.local for local overrides (optional).
# Production: set VITE_API_URL in Vercel dashboard.

# Leave empty to use Vite dev proxy (localhost:5173 -> localhost:8000)
VITE_API_URL=

# Example for production preview against Render:
# VITE_API_URL=https://sifi-api.onrender.com
